package lists;

import java.util.ArrayList;

public class ArrayGoogle {
    public <Interval> void insert(ArrayList<Interval> intervals, Interval newInterval) {
        if (intervals == null || intervals.size() == 0);


    }
}
